# Tasks — Current Phase Only

> Only the current phase gets detailed tasks. Future work lives in ROADMAP.md.
> /rdw-end archives this file to ops/archive/ before mutating it.

## Active
- [ ] Review and merge PR #1 (`rdw-dotfiles-scaffold` → `master`).
- [ ] Decide on `M shell/.zshrc` and untracked `.agents/` (unrelated to PR #1).

## Ideas
- Pick the next roadmap phase once PR #1 lands (e.g. dogfood the RDW flow on the next
  real desktop change).

## Questions
- <none right now>
